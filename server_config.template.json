{
    "host": "localhost, buildblast.com, etc",
    "port": 8080, // port to listen on. Web uses this port, server uses port+1
    "client_assets": "path to client assets, usually '.'",
    "persist_enabled": true,
    "world_base_dir": "path the world persistance data will be saved",

    "db_pass": "password",
    "mail_pass": "password",
    "cookie_key_pairs": [
        "authentication key",
        "encoding key"
    ]
}